
<%= @portfolios.each do |p| %>
  <div><%= link_to 'update', edit_portfolio_path(p) %></div>
  <p><%= p.title %></p>
  <p><%= p.subtitle %></p>
  <p><%= p.body %></p>
  <%= image_tag p.thumb_image if !p.thumb_image.nil? %>
  <hr>
<% end %>
